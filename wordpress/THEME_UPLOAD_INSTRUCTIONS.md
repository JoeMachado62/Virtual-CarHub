# Theme Upload Instructions

Place the Motors theme zip at:

- `wordpress/uploads/motors-theme.zip`

After upload, the next implementation step will be:

1. Extract theme files into a working directory.
2. Create a child theme in-repo for versioned custom templates.
3. Wire inventory archive + VDP templates to Virtual-CarHub export data model.
